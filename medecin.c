#include <stdio.h>
#include <stdlib.h>
#include "asem.h"
#include "shm.h"

#define TAILLE_NOM_MAX 10

int main (int argc, char** argv)
{
	if (argc > 1)
	{
		fprintf(stderr, "Usage: %s", argv[0]);
		exit(1);
	}


}
