#include <stdlib.h>

int main (int argc, char** argv)
{
	if (argc > 1)
	{
		fprintf(stderr, "Usage: %s", argv[0]);
		exit(1);
	}

	//if (sem != NULL)
	//		sem_destroy()

	//if (mem != NULL)
	//		mem_destroy()
}
